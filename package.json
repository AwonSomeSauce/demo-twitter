{
  "name": "app",
  "private": "true",
  "scripts": {
    "build:css": "npx sass ./app/assets/stylesheets/application.scss ./app/assets/builds/application.css --no-source-map --load-path=node_modules"
  },
  "devDependencies": {
    "sass": "^1.81.0"
  }
}
